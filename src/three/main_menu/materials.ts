import * as THREE from "three";

export const TowerMaterial = new THREE.MeshStandardMaterial({
    color: "#000",
    metalness: 0.4,
    roughness: 0.2,
});

export const THREEMainColor = new THREE.Color("#c44569");
